<nav class="navbar navbar-expand-lg navbar-dark fixed-top p-0">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Liverpool One</a>
    <button type="button" class="navbar-toggler" data-bs-toggle="offcanvas" data-bs-target="#offcanvasNavbar" aria-expanded="false" aria-controls="offcanvasNavbar">
      <% if current_user.avatar.present? %>
        <%= image_tag current_user.avatar.dropdown.url, class: 'dropdown-user-icon' %>
      <% else %>
        <%= image_pack_tag 'default.jpg', class: 'dropdown-user-icon', size: '50x50' %>
      <% end %>  
    </button>
    <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasNavbar" aria-labelledby="offcanvasNavbarLabel">
      <div class="offcanvas-header">
        <h5 class="offcanvas-title" id="offcanvasNavbarLabel">Liverpool One</h5>
        <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="閉じる"></button>
      </div>
      <div class="offcanvas-body">
        <ul class="navbar-nav justify-content-end flex-grow-1 pe-3">
          <li class="nav-item">   
            <% if current_user.avatar.present? %>
              <%= image_tag current_user.avatar.dropdown.url, class: 'dropdown-user-icon' %>
            <% else %>
              <%= image_pack_tag 'default.jpg', class: 'dropdown-user-icon', size: '50x50' %>
            <% end %>
          </li>
          <li class="nav-item">    
            <%= link_to 'マイページ', user_path(id: current_user.id), class: 'nav-link active' %>
          </li>
          <li class="nav-item">
            <%= link_to 'プロフィール変更', edit_user_registration_path, class: 'nav-link active' %>
          </li>
          <li>
            <hr class="divider text-white">
          </li>
          <!--ログインしてるユーザーが管理者の場合のみ-->
          <% if current_user.try(:admin?) %>
            <li class="nav-item">
              <%= link_to '管理者ログイン', rails_admin_path, class: 'nav-link active' %>
            </li>
          <% end %>
          <li class="nav-item">
            <%= link_to 'ログアウト', destroy_user_session_path, class: 'nav-link active', method: :delete %>
          </li>
        </ul>
      </div>
    </div>
  </div>
</nav>